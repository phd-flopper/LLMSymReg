We are performing a symbolic regression analysis of some dataset. Our goal is to uncover the governing function for this dataset. This function should be simple and meaningful.

Your collegue "Planner" proposed these `constraints`: {constraints}.
As part of `constraints`, your colleague "Planner" also proposed this functional form: {template}.
Here are the current parameters of the PySR generator: {params}.
Here are the default values, for your reference: {basic_params}.
Here are the current equations generated by PySR: {eqs}.
At least one of the equations should generally contain all variables. Here's the list: {vars_}.
Please consider the following report: {telemetry}.

Based on the report, please formulate updated parameters for the PySR generator. Output a JSON object with your proposed new values of the parameters to enhance the search. YOUR OUTPUT JSON SHOULD ONLY CONTAIN KEYS SPECIFIED IN THE EXAMPLE BELOW.

Your output MUST be a single, valid JSON object containing the aforementioned keys and their respective correct values. You should only generate one instance of the JSON object.
Here is a small example:
### JSON Output Structure:
```json
{{
  "maxsize": your proposed integer value here,
  "maxdepth" your proposed integer value here,
  "binary_operators": your proposed list here,
  "unary_operators": your proposed list here
}}

Here's detailed guidance to these tunable parameters:

"maxsize" is an integer that specifies the maximum complexity of the equation. Default value is 15, reasonable maximum value is up to 40. Consider increasing this parameter slightly (by no more than 5) if you find that equations are not complex enough or if NONE of them contain ALL variables. Consider decreasing this parameter if the equations get too long and too complex. Note that this parameter SHOULD NEVER be less than 7. It SHOULD NEVER be more than 60. Also note, that increasing "maxsize" increases runtime, hence only consider it if the equation generation process is not going well.

"maxdepth" specifies the maximum depth of the equation's expression tree. 12 is a good value unless your domain knowledge suggests that the equation is very complex. "maxdepth" value SHOULD NEVER exceed "maxsize" value. Consider reducing "maxdepth" (up to a minimum of 3) if based on your analysis and "Planner"'s constraints, the true function is short. Consider increasing the value (up to your proposed 'maxsize') if the true function should be long.

"binary_operators" is a list specifying available binary operators. Your possible output is any non-empty subset of ['+', '-', '*', '/', '^']. Consider removing operators if "Planner" suggests they are not needed. For example, '/' is not needed if we are dealing with strictly polynomials. Consider adding operators if "Planner" suggests we need them. For example, if "Planner" suggests that we are dealing with exponentiation only, you should add '^'. Remember, that the list of operators SHOULD NEVER be empty and SHOULD NEVER exceed the possible output subset.

"unary_operators" is a list specifying available unary operators. Your possible output is any (including empty this time) subset of ['sin', 'cos', 'log', 'exp', 'sqrt']. Consider removing unary operators if "Planner" suggests they are not needed. If "Planner" suggests no unary operators, you might even suggest an empty list []. Consider adding unary operators if they are needed based on "Planner"'s constraints. This list SHOULD NEVER exceed the possible output. Do not introudce new unlisted unary operators.

Your output JSON MUST ONLY contain the mentioned keys and their values.

After your JSON object output, I want you to formulate ideas for the directions of improvement in textual form. Base your ideas on your analysis of the report, of the dataset and of "Critic"'s constraints. These ideas should follow from your analyzis of the report and the dataset and your expert knowledge. Your ideas should include ideas and suggestions about: symmetry or lack of thereof, corner cases and domain constraints. You should write your proposed ideas and reasoning for them after first writing ### IDEAS.